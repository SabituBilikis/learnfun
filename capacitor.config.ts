import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.learnfun.app",
  appName: "Learn Fun",
  webDir: "dist",
  android: {
    backgroundColor: "#FFFFFF",
  },
};

export default config;
