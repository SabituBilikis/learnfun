import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.learnfunkids.app",
  appName: "Learn Fun",
  webDir: "dist",
  android: {
    backgroundColor: "#FFFFFF",
  },
};

export default config;
