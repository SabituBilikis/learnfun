# Keep the real keystore and this file's populated copy outside version control.
# Relative paths resolve from the android directory.
storeFile=../learnfun-release.keystore
storePassword=replace-with-keystore-password
keyAlias=learnfun
keyPassword=replace-with-key-password
